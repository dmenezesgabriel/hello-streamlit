import React from "react"
import ReactDOM from "react-dom/client"
import PlotlyEvents from "./PlotlyEvents"

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PlotlyEvents />
  </React.StrictMode>
)
